# --- added by remote_opencode_sync ---
opencode-history/
# --- Dependencies (each machine installs its own) ---
node_modules/
.bun/
vendor/
.venv/
venv/
__pycache__/
*.pyc

# --- Builds / generated ---
dist/
build/
out/
target/
*.tsbuildinfo

# --- Environment & secrets (never commit; commit only .env.example) ---
.env
.env.*
!.env.example
*.local
*.local.*

# --- Machine-local state ---
*.sock
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Session/tool state kept out of sync (if it ever appears in-project) ---
.opencode/state/