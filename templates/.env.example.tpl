# Copy this file to `.env`, fill in real values, and keep `.env` gitignored.
# Each machine keeps its own `.env` — never commit real secrets.

# Example:
# API_KEY=sk-...
# DATABASE_URL=postgres://user:pass@host:5432/db